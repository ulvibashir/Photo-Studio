import React from 'react';
import { View } from 'react-native';

export const StudioListItem = () => {
  return(
      <View>
          {/* Each studio will render with this item in flatlist (in StudioListBody) */}
      </View>
  )
}